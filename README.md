# Simple Subscription Management API

Tech to use:
- Flask
- SQLite
- SQLAlchemy

Command
```bash
pip install -r requirements.txt
```

# Run test
```bash
pip install pytest pytest-cov
pytest --cov=../subscription_management_api --cov-report=term-missing
```